// Enum
enum OptionType {
  useDefault,
  useConfigure,
}

enum FieldRequire {
  single,
  dual,
}

enum ConnectionStatus {
  great,
  good,
  poor,
  disconnected,
}

enum ZoneStatus {
  outside,
  inside,
}

const String fontName = 'Poppins';